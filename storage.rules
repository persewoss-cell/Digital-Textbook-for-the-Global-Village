rules_version = '2';

// Firestore 규칙과 같은 이유로, 여기서도 "우리 앱을 거쳐 들어왔는지(익명 인증)"만 확인합니다.
service firebase.storage {
  match /b/{bucket}/o {
    match /textbooks/{grade}/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
}
